@using WidlyApp2.Models

<h2>@Model.Name</h2>

<ul>

    <li>
        Membership Type: @Model.MembershipType.Name
    </li>
    @if(Model.BirthDate != null)
    { 
    <li>
        Birth Day: @Model.BirthDate.Day/@Model.BirthDate.Month/@Model.BirthDate.Year
    </li>
    }
</ul>